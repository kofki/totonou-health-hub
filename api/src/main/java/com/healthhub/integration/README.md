# integration/ — Claude territory (Phase 2)

Receives normalized health data pushed by the Python `ingestion/` worker, once the owner
has designed the ingestion→API contract (P2-03). Retry/rate-limit glue lives here too.
