# controller/ — owner first, Claude boilerplate later

Owner writes the first controllers while learning. After entities exist, Claude may add
meal-library CRUD boilerplate (P5-02) on a `claude/` branch for owner review.
