plugins {
    // Example: lets Gradle auto-download the JDK 21 toolchain declared in build.gradle
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'health-hub-api'
